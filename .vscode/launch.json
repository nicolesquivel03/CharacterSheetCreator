{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Chrome",
            "url": "http://localhost:4200",
            "request": "launch",
            "type": "chrome",
            "webRoot": "${workspaceFolder}"
        }
    ]
}